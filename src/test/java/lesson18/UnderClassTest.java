package lesson18;

import org.junit.jupiter.api.Test;

public class UnderClassTest {

    @Test
    public void testAdd() {
//        // set
//        IListener mock = createMock(IListener.class);
//        UnderClass cl = new UnderClass();
//        cl.addDocument(mock);
//
//        // replay
//        expect(mock.docAdded(anyObject()));
//        // вместо anyObject() есть:
//        // andEq(new Document); - проверка на equals
//        // arrEq() - массив
//        // isA(String.class); - правильность класса
//        // примеры методов
//        // expect(mock.docAdded(anyObject())).andReturn(true);
//        // expect(mock.docAdded(anyObject())).andReturn(true).times(10);
//        // expect(mock.docAdded(anyObject())).times(10);
//        // expect(mock.docAdded(anyObject())).times(10);
//        // expect(mock.docAdded(anyObject())).andThrow(new Exception);
//        //
//        replay(mock);
//
//        // verify
//        cl.addDocument(Document);
//        verify(mock);
    }

}
